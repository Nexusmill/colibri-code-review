{
  "_doc": "Remediation manifest (machine-readable, jCodemunch-indexed). Human-readable view: docs/REMEDIATION_MANIFEST.md.",
  "_convention": "Every fix of a real defect (security/correctness/crash/leak/race/injection/data-loss) gets an entry IN THE SAME COMMIT that fixes it. A review flagging already-fixed code is recorded as status='verified-stale', never re-fixed (G1/G11). Hygiene/refactor items are NOT remediations. Re-run index_file on this JSON after edits.",
  "sources": [
    "in-session colibri review (GLM-5.3)",
    "perfection protocol run (PERFECTION_PROTOCOL v3)"
  ],
  "entries": [
    {
      "date": "2026-09-26",
      "commit": "pending (same commit as this row)",
      "product": "colibri",
      "file": "analyzer.py (load_spec)",
      "severity": "MEDIUM",
      "finding": "CLR-W2-FDLEAK (bug review 2026-09-25 @ sha 8afa3ca9, CONFIRMED, still open on 2026-09-26 bytes): load_spec opened the spec source with a bare `open(source, encoding=\"utf-8-sig\", errors=\"ignore\").read()` and never closed the handle - repeated spec loads in batch use exhaust file descriptors.",
      "fix": "`with open(...) as f: raw = f.read()` - deterministic close on every path including read failure (exception still propagates; the with-block only guarantees close, never swallows).",
      "verification": "RED-first tests loadspec_handle_closed / loadspec_readfail_still_closes / loadspec_readfail_propagates in tests/test_analyzer.py (failed on pre-fix bytes, green post-fix); mutation check M-I1 (revert to bare open) turns them RED again; full battery 4/4.",
      "source": "colibri bug review 2026-09-25 (carried finding) -> perfection(analyzer.py) wave 2"
    },
    {
      "date": "2026-09-26",
      "commit": "pending (same commit as this row)",
      "product": "colibri",
      "file": "analyzer.py (model_max_tokens)",
      "severity": "LOW",
      "finding": "CLR-W2-SOCKLEAK (bug review 2026-09-25 @ sha 8afa3ca9, CONFIRMED): `json.load(urllib.request.urlopen(req, timeout=20))` never closed the HTTP response; mitigated in practice by _MODEL_MAX_CACHE (one call per model per process) but still one leaked socket per uncached model, and the except-path leaves nothing to collect. Sibling precedent done right: app.py:21 `with urllib.request.urlopen(...) as r:`.",
      "fix": "`with _u.urlopen(req, timeout=20) as resp:` wrapping the model scan loop - response closed on success, on no-match, and on any parse/transport exception before the except guard.",
      "verification": "RED-first tests mmt_response_closed / mmt_value_parsed / mmt_fetchfail_fallback (the fetch-failure fallback 131072 path pinned unchanged); mutation M-I2 turns the close check RED; battery 4/4.",
      "source": "colibri bug review 2026-09-25 (carried finding) -> perfection(analyzer.py) wave 2"
    },
    {
      "date": "2026-09-26",
      "commit": "pending (same commit as this row)",
      "product": "colibri",
      "file": "analyzer.py (review_code)",
      "severity": "MEDIUM",
      "finding": "CLR-W2-AUTO-DIVERGENCE (NEW, perfection L2 pass A 2026-09-26 @ sha 8afa3ca9): the :317 comment documented max_tokens AUTO as `None/<=0` but the code `int(_mt) if _mt` only mapped None/0 to the model ceiling; a negative value - reachable via `run_batch --max-tokens -5` (argparse type=int, no floor) - was sent to the API and failed 400 per file instead of using the ceiling, violating the file's own documented contract.",
      "fix": "`max_tokens = int(_mt) if (_mt or 0) > 0 else model_max_tokens(...)` - the comment's None/<=0 contract is now literally the predicate; input partition {None, 0, -5} -> ceiling, {5000} -> 5000.",
      "verification": "RED-first test auto_negative_uses_ceiling (failed on pre-fix bytes: create was called with max_tokens=-5); mutations M-I4 (revert predicate) RED-OK; auto_none/auto_zero/explicit pins from wave 1 unchanged green; battery 4/4.",
      "source": "perfection(analyzer.py) L2 pass A 2026-09-26 (fresh delta over prior reviews) -> wave 2"
    },
    {
      "date": "2026-09-26",
      "commit": "pending (same commit as this row)",
      "product": "colibri",
      "file": "analyzer.py (review_code mode gate)",
      "severity": "MEDIUM",
      "finding": "CLR-W3-MODECOERCE (quality review 2026-09-26 @ 8afa3ca9): an unknown `mode` was silently coerced to \"bug\" - a caller typo (e.g. \"quaity\") produced a billed bug review with no signal. MODES is the product's whole mode vocabulary; the gate is this file's to enforce. Caller audit: app.py builds modes from constants, run_batch restricts via argparse choices, legacy drivers pass literals - no live caller can send an invalid mode, so raising is safe today and typo-proof tomorrow.",
      "fix": "Loud ValueError before any spend: `raise ValueError(\"unknown mode %r; expected one of %s\" % (mode, sorted(MODES)))` replacing the coercion - message names the bad value and the full valid set; raised above the key check so no client is ever constructed.",
      "verification": "RED-first tests mode_rejected_'quaity'/''/'BUG'/None + mode_reject_no_client (5 checks failed on pre-fix bytes; message asserted to contain every MODES name per the local-gate amendment); all five valid modes still dispatch their own _SYS prompt (spec with expectations embedded); mutation M-W3 reverts to coercion and turns all 5 RED; battery 4/4.",
      "source": "quality review 2026-09-26 (tranche 3) -> perfection(analyzer.py) wave 3; local gate hy4 BLOCK on vacuous T1 amended (assert all five mode names), grok-4.3 CLEAR"
    },
    {
      "date": "2026-09-26",
      "commit": "pending (same commit as this row)",
      "product": "colibri",
      "file": "analyzer.py (_call_retry/_transient + review_code wiring)",
      "severity": "MEDIUM",
      "finding": "CLR-W4-NORETRY (quality review 2026-09-26 @ 8afa3ca9): a single transient API error (429/5xx/connection blip) mid-batch marked the file errored - run_batch records status:error, spend on prior files stands, and a re-run re-pays input tokens for the whole file set unless shas changed. Direct cost consequence for a tool whose premise is cheap triage.",
      "fix": "Bounded transient retry: _transient() recognizes RateLimitError(429)/APIStatusError>=500/APIConnectionError/APITimeoutError (verified against installed openai 3.19.2); _call_retry() wraps BOTH the primary call and the corrective JSON-retry call - after a failed attempt a it sleeps retry_backoff_base * 2**(a-1) (defaults 3 attempts / 1.0s base = 3.0s worst added wall time), re-raises non-transient errors at once, propagates the last transient exception when the budget is exhausted. Knobs are cfg-driven per the file's constitution (retry_max_attempts=1 disables).",
      "verification": "RED-first: 7 checks failed on pre-fix bytes (ratelimit/5xx/conn recovery, usage-from-success-only, exhaustion-raises-last, backoff shape, JSON-retry composition). Post-fix 94/94 incl. non-transient-never-retried pins (BadRequestError/plain RuntimeError) and knob-off single-attempt; backoff shape asserted via sleep accumulator [0.001, 0.002]; mutations M-W4a (drop 429 clause) and M-W4b (ignore budget) each RED; battery 4/4. AMENDED same session (EV-136 commit-gate BLOCK remediated): _transient made total (getattr+None guard; attribute-less APIStatusError subclass is non-transient), max_attempts clamped >=1, base clamped >=0 with a 60s per-sleep cap; 15 new checks incl. 5 RED-first guard tests and direct _transient unit tests; M-W4R-1/2 RED-OK; net 109/109; speculative findings (string status_code, jitter, 501/505 exclusion) rebutted with reasons via run --context.",
      "source": "quality review 2026-09-26 (tranche 4) -> perfection(analyzer.py) wave 4; local gate grok-4.3 rounds 1-2 BLOCK (vacuous I4, formula-notation conflict) amended, round 3 CLEAR"
    },
    {
      "date": "2026-09-26",
      "commit": "pending (same commit as this row)",
      "product": "colibri",
      "file": "analyzer.py (review_code client, model_max_tokens cache, load_spec filter)",
      "severity": "MEDIUM",
      "finding": "CLR-W7-STAGEE-BLIND (hy4-preview Stage-E blind review @ 8813baf2... sha 8818baf2, CONFIRMED by Phase-3): (1) the OpenAI client constructed per review_code call was never closed - pool ownership left to GC timing (SDK __enter__/__exit__/close verified on openai 3.19.2); (2) _MODEL_MAX_CACHE keyed by model only while lookups take base_url - the file's live-per-call api_base means switching providers in one process served the wrong ceiling for the same model id; (3) load_spec compared raw registry id values against the string ids filter, so numeric-id registries never matched and spec mode raised 'no matching spec rows' for rows that exist. None of the three were in the local record - the author-blind-spot residue the protocol's external stage exists to catch.",
      "fix": "(1) `with OpenAI(...) as client:` wrapping the remainder of review_code - close guaranteed on every path incl. transient-exhausted raises; (2) cache key is now the tuple (model, base_url, fallback) - failure-caching of the high-safe fallback deliberately KEPT (fallback never truncates a review; re-attempting a dead network would stall batches 20s/file - adjudicated in wave7_contract.md); (3) rid = str(r.get(\"id\", \"?\")) - numeric ids match the filter and render identically.",
      "verification": "RED-first: client_closed_on_return/exception_path, cache_distinct_base_new_lookup (progressive call-count), spec_numeric_id_filter/render all failed on pre-fix bytes. Post-fix 122/122; mutations M-W7b/M-W7c RED-OK and M-W7a as a whole-wave revert (E1-only revert not byte-expressible without re-indentation - deviation logged) with client_closed_on_return among the REDs; battery 4/4; two test prime-sites moved to tuple keys per contract.",
      "source": "perfection(analyzer.py) Stage E blind phase (hy4-preview, $0.10, 495s) -> Phase-3 CONFIRMED -> wave 7; reconcile round: all local-record entries AGREE, zero re-raisals"
    },
    {
      "date": "2026-09-26",
      "commit": "pending (same commit as this row)",
      "product": "colibri",
      "file": "analyzer.py (load_spec clause render, _build_prompt prior block, _validated_json finish)",
      "severity": "MEDIUM",
      "finding": "CLR-W8-COLD-BLIND (grok-4.6 effort-high Stage-E COLD pass, BLIND, COLD VERDICT: FINDINGS; all three Phase-3 CONFIRMED): (1) load_spec's `if v:` dropped False/0 contract-clause values exactly like the deliberately-skipped empty string - the quiet clauses (disabled states, zero limits) spec mode exists to judge silently vanished from expectations; (2) plan mode + prior_md attached the DELTA block ('Report ONLY findings that are NEW or CHANGED... do not restate') directly contradicting the plan template's 'plan for exactly its still-open findings' - the colibri console's plan-with---findings path ships this combination; (3) a successful corrective JSON retry left usage['finish'] at the FIRST response's reason, so a length-truncated attempt that parsed on retry still reported finish='length'.",
      "fix": "(1) clause render predicate `if v is not None and v != \"\"` (False/0 render; the pinned empty-string skip kept - NOT grok's blanket `is not None` which would emit empty clauses) and `elif r.get('expected') not in (None, '')`; (2) mode-aware prior block in _build_prompt - plan mode gets plan-contract attach wording, every other mode keeps the delta block byte-identical; (3) usage['finish'] = r2.choices[0].finish_reason on corrective success (json_error path keeps the first response's finish, consistent with kept raw content).",
      "verification": "RED-first: spec_false/zero/falsy-expected render, plan_prior_no_delta_wording, retry_finish_updated all failed on pre-fix bytes (plan_prior_keeps_still_open was green pre-fix via the plan template's own wording - its partner check carries the contradiction). Post-fix 129/129; mutations M-W8a/b/c RED-OK; battery 4/4; pyflakes 0. AMENDED same session (EV-137 commit-gate BLOCK): finish now tracks the response whose content shipped AND the first attempt's finish is preserved as usage['first_finish'] when a retry ships (test asserts both; the additive resolution of the gate/cold-pass semantic dispute - F1/F3 accepted, F2 judged cosmetic by the gate itself).",
      "source": "perfection(analyzer.py) Stage E COLD pass (grok-4.6 high, $0.09, 343s) -> Phase-3 CONFIRMED -> wave 8 (E.5 re-entry)"
    },
    {
      "date": "2026-09-26",
      "commit": "pending (same commit as this row)",
      "product": "colibri",
      "file": "analyzer.py (review_code client ctor, load_spec registry guards)",
      "severity": "MEDIUM",
      "finding": "CLR-W9-COLD2 (grok-4.6 effort-high Stage-E COLD pass 2, BLIND, COLD VERDICT: FINDINGS): (C4 CONFIRMED) the openai SDK client defaults to max_retries=2 - its own retry of the same transient classes - stacking with _call_retry's budget of 3: up to 9 HTTP requests for an advertised budget of 3, with doubled backoff aggravating the 429s being retried; the cfg knobs did not mean what they said. (C6 CONFIRMED) load_spec crashed with AttributeError on malformed registries (controls/features as a non-list object iterating dict keys; non-dict rows hitting r.get on str/None/None) instead of the documented pass-through/skip. (C5 DISPUTE UPHELD, no change) failure-caching of the high-safe 131072 fallback re-raised from the long-running-UI side - upheld per the wave-7 adjudication (a stale 131072 truncates nothing in practical use; not-caching re-attempts dead network at 20s/file across batches; Authorization-on-/models sub-point to owner backlog, OpenRouter endpoint is public).",
      "fix": "(C4) the client is constructed with max_retries=0 - _call_retry is the single retry owner and retry_max_attempts is now the TOTAL HTTP budget (DEFAULTS comment updated); (C6) non-list controls/features passes through as raw stripped text and non-dict rows are skipped, both never crashing.",
      "verification": "RED-first: sdk_max_retries_zero, spec_nonlist_controls_passthrough, spec_nondict_rows_skipped failed on pre-fix bytes. Post-fix 132/132; mutation M-W9a RED-OK; M-W9b caught at function granularity (rc=1 under mutation via the ValueError escape, 132/132 on restore - check-level attribution documented); battery 4/4; pyflakes 0.",
      "source": "perfection(analyzer.py) Stage E COLD pass 2 (grok-4.6 high, $0.11, 469s) -> Phase-3 (2 CONFIRMED, 1 dispute upheld) -> wave 9"
    },
    {
      "date": "2026-09-26",
      "commit": "pending (same commit as this row)",
      "product": "colibri",
      "file": "analyzer.py (load_spec row-header fields)",
      "severity": "LOW",
      "finding": "CLR-W10-COLD3-COMPLETION (qwen/qwen3.8-flash Stage-E cold pass 3, owner-picked 3rd family, $0.027: claimed 4 HIGH/CRIT + 6 MED; Phase-3 adjudicated ALL MED+ refuted/adjudicated for the single-user-local-tool trust model - full table in junk/perfection_waves/cold3_adjudication.md, gate-cleared): the ONE accepted slice - load_spec's row-header fields still had the truthiness disease wave 8 cured for clause values (label 0/False fell through to feature, status 0/False silently dropped, an empty contract dict swallowed the row's expected) because the wave-8 sibling sweep wrongly declared itself complete without these fields.",
      "fix": "Presence-checks matching wave 8's predicate style: label renders verbatim unless None/empty (then the feature fallback); status renders unless None/empty; `isinstance(c, dict) and c` so an empty contract falls through to the expected branch.",
      "verification": "RED-first: spec_falsy_label_renders ('### z1 - 0'), spec_falsy_status_renders ('STATUS: False'), spec_empty_contract_renders_expected all failed on pre-fix bytes. Post-fix 135/135; mutations M-W10a/b RED-OK; battery 4/4; pyflakes 0.",
      "source": "perfection(analyzer.py) Stage E cold pass 3 (qwen3.8-flash medium, 546s) -> Phase-3 split verdict -> wave 10 (micro)"
    }
  ]
}
