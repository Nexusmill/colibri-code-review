{
 "type": "CLEAR",
 "commit": "4e48c28e7755b93823d05f701650ebdeeed58754",
 "caller": "zcode",
 "when": "20260926-103958",
 "files": {
  "analyzer.py": {
   "sha": "02d65d39e8351c5fd772bc25dbbb74a30b5e4cac8ebbc2dc273087e04eb6e364",
   "model": "deepseek/deepseek-v4-pro-0813@deepinfra/fp8+wafer+ionstream",
   "when": "20260926-103954",
   "artifact": ".adversary\\reviews\\gate_20260926-103954.md"
  },
  "docs/remediation_manifest.json": {
   "sha": "1f196c83ea1edebedde92daba98f4bbaf7652d186fa0c6fd19971f69226a02c0",
   "model": "deepseek/deepseek-v4-pro-0813@deepinfra/fp8+wafer+ionstream",
   "when": "20260926-103954",
   "artifact": ".adversary\\reviews\\gate_20260926-103954.md"
  },
  "tests/test_analyzer.py": {
   "sha": "d8bf86eaa0fea920bfcd6b5ff1554ca8ff4497eb08bcfb4f0612d3abcb419055",
   "model": "deepseek/deepseek-v4-pro-0813@deepinfra/fp8+wafer+ionstream",
   "when": "20260926-103954",
   "artifact": ".adversary\\reviews\\gate_20260926-103954.md"
  }
 },
 "artifacts": {
  ".adversary\\reviews\\gate_20260926-103954.md": "model: deepseek/deepseek-v4-pro-0813@deepinfra/fp8+wafer+ionstream | provider: DeepInfra | usage: {'prompt_tokens': 22025, 'completion_tokens': 1732, 'total_tokens': 23757, 'cost': 0.0331357, 'is_byok': False, 'prompt_tokens_details': {'cached_tokens': 0, 'cache_write_tokens': 0, 'audio_tokens': 0, 'video_tokens': 0}, 'cost_details': {'upstream_inference_cost': 0.0331357, 'upstream_inference_prompt_cost': 0.0286325, 'upstream_inference_completions_cost': 0.0045032}, 'completion_tokens_details': {'reasoning_tokens': 0, 'image_tokens': 0, 'audio_tokens': 0}} | scrubbed: 0 | files: ['analyzer.py', 'docs/remediation_manifest.json', 'tests/test_analyzer.py'] | caller: zcode\n\n## Adversarial Review\n\nThe change adds `max_retries=0` to the OpenAI client constructor and guards `load_spec` against malformed registries. Let me verify each claim.\n\n### 1. `max_retries=0` in client constructor\n\nThe diff adds `max_retries=0` to the `OpenAI(...)` call. The test `test_sdk_retry_disabled` checks `ctor[0].get(\"max_retries\") == 0`. This is straightforward and correct \u2014 the SDK's default retry would indeed stack with `_call_retry`.\n\n**However**, I need to check: does the `OpenAI` constructor actually accept `max_retries` as a keyword argument? Looking at the openai SDK (v3.x), the `OpenAI.__init__` signature includes `max_retries: int | None = None` (or similar). The test fakes the constructor entirely (`an.OpenAI = _ctor`), so it only verifies the kwarg is *passed*, not that the real SDK accepts it. But the manifest claims \"verified against installed openai 3.19.2\" \u2014 the SDK does accept `max_retries` in its constructor. This is fine.\n\n### 2. `load_spec` malformed registry guards\n\nThe diff adds:\n```python\nif not isinstance(rows, list):\n    return raw.strip()                     # malformed registry: pass through, never crash\n```\nafter extracting `rows`, and:\n```python\nif not isinstance(r, dict):\n    continue                           # malformed row: skip, never crash\n```\ninside the loop.\n\nLet me trace through the test cases:\n\n**Test 1: `bad = '{\"controls\": {\"id\": \"1\"}}'`**\n- `d = json.loads(raw)` \u2192 `{\"controls\": {\"id\": \"1\"}}`\n- `rows = (d.get(\"controls\") or d.get(\"features\") or [])` \u2192 `{\"id\": \"1\"}` (truthy dict)\n- `isinstance(rows, list)` \u2192 `False` \u2192 returns `raw.strip()` = `'{\"controls\": {\"id\": \"1\"}}'`\n- Test expects `got == bad` \u2014 but `bad` is the raw string `'{\"controls\": {\"id\": \"1\"}}'` and `raw.strip()` returns the same. \u2713\n\n**Test 2: `mixed = {\"controls\": [None, 1, {\"id\": \"ok\", \"label\": \"L\", \"expected\": \"e\"}]}`**\n- `rows = [None, 1, {\"id\": \"ok\", ...}]` \u2014 a list, passes the guard\n- Loop: `r = None` \u2192 `not isinstance(r, dict)` \u2192 `continue` \u2713\n- `r = 1` \u2192 `not isinstance(r, dict)` \u2192 `continue` \u2713\n- `r = {\"id\": \"ok\", ...}` \u2192 renders \u2713\n- Test expects `\"### ok - L\" in out` and `\"EXPECTED: e\" in out` \u2713\n\nBoth guards work as claimed.\n\n### 3. Potential issue: `rows` extraction with non-dict `d`\n\nWait \u2014 the guard `if not isinstance(rows, list)` is placed *after* the extraction. But what if `d` is a list (not a dict)? The extraction line is:\n\n```python\nrows = (d.get(\"controls\") or d.get(\"features\") or []) if isinstance(d, dict) else []\n```\n\nIf `d` is a list, `rows = []`, which passes `isinstance(rows, list)` and then `if not rows: return raw.strip()`. That's fine.\n\nBut what if `d` is a dict where `controls` is a non-list, non-dict value like `\"controls\": 42`? Then `rows = 42`, `isinstance(rows, list)` is `False`, returns `raw.strip()`. Fine.\n\nWhat about `\"controls\": None`? Then `d.get(\"controls\")` is `None`, `None or d.get(\"features\") or []` \u2192 falls through to `features` or `[]`. Fine.\n\n### 4. The `rid = str(r.get(\"id\", \"?\"))` line\n\nThis was already in the code before this diff (from wave 7). The diff doesn't touch it. Not a finding.\n\n### 5. Manifest entry consistency\n\nThe manifest entry for W9 claims:\n- \"C4 CONFIRMED\" \u2014 the SDK retry stacking issue\n- \"C6 CONFIRMED\" \u2014 the load_spec crash on malformed registries\n- \"C5 DISPUTE UPHELD, no change\" \u2014 failure-caching of fallback\n\nThe fix section says: \"(C4) the client is constructed with max_retries=0... (C6) non-list controls/features passes through as raw stripped text and non-dict rows are skipped, both never crashing.\"\n\nThe code matches the manifest claims.\n\n### 6. Test registration\n\nThe new tests `test_sdk_retry_disabled` and `test_malformed_registry_guard` are added to the `main()` test list. \u2713\n\n### 7. Edge case: `load_spec` with `controls` as a non-list but truthy value\n\nConsider `{\"controls\": \"not-a-list\"}`:\n- `rows = \"not-a-list\"` (truthy string)\n- `isinstance(rows, list)` \u2192 `False` \u2192 returns `raw.strip()` \u2713\n\nConsider `{\"controls\": 0}`:\n- `rows = 0 or d.get(\"features\") or []` \u2192 `[]` (since 0 is falsy)\n- `isinstance([], list)` \u2192 `True`, `if not rows: return raw.strip()` \u2713\n\nConsider `{\"controls\": {\"nested\": \"dict\"}}`:\n- `rows = {\"nested\": \"dict\"}` (truthy)\n- `isinstance(rows, list)` \u2192 `False` \u2192 returns `raw.strip()` \u2713\n\nAll handled.\n\n### 8. One subtle concern: the `_run` helper and `ctor` recording\n\nIn `test_sdk_retry_disabled`, the test calls `_run([_Resp(\"ok\")])` which sets `an.OpenAI = _ctor` where `_ctor` appends kwargs to `ctor` and returns the fake. The test then checks `ctor[0].get(\"max_retries\") == 0`. This works because `review_code` calls `OpenAI(api_key=key, base_url=..., default_headers=..., max_retries=0)` and `_ctor` captures all kwargs. \u2713\n\n### 9. Does `max_retries=0` actually disable SDK retries?\n\nIn the openai SDK, `max_retries=0` means zero retries (the SDK's `_make_status_error` and retry logic checks `max_retries`). The manifest claims this was verified against openai 3.19.2. The test only verifies the kwarg is passed, not the SDK behavior, but that's acceptable for an offline test \u2014 the SDK's behavior is the SDK's contract.\n\n### 10. Any interaction with the existing `_call_retry`?\n\n`_call_retry` wraps `_call` which calls `client.chat.completions.create(...)`. With `max_retries=0` on the client, the SDK won't retry internally. `_call_retry` handles retries at the application level. This is the intended design. No conflict.\n\n### 11. The `DEFAULTS` comment change\n\nThe comment for `retry_max_attempts` is updated to clarify it's the TOTAL HTTP budget. This is documentation-only, no functional change.\n\n---\n\nI find no defects introduced by this change. The guards are correctly placed, the tests are properly registered, the manifest entry is consistent with the code, and the `max_retries=0` kwarg is correctly passed.\n\nVERDICT: CLEAR"
 }
}
