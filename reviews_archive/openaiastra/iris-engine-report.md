# Iris engine and solid terrain handoff

Frozen5files in engine-manifest.json: engine.js,terrain.js,engine.test.cjs,spectrum.test.cjs,terrain.test.cjs. engine.diff normalized-lines review view; exact candidate hashes authoritative. gardens.js/spectrum.js are byte-identical production dependencies; no edits. All scratch code writes use canonical Tools safe_write. Production untouched; no commit/review clearance claimed.

## Diagnosis and changes
Blackpad failure reproduced at x139,y485 (29 from center), sixteen blackcells, no input, submarine gardens15/40: five removed, eleven remain, x141.88378 after60s. Once some gravity lifts, current drifts outside radius30 and resets dwell. Other modes removed16. Fix stops inertia/current only while actualstationoverlap and rawinput magnitude<=.05; steering restores ordinary motion (no lock/snap). Regression proves exact180frames percell, all16 removed2880frames/48seconds while coordinates unchanged, followed by movement on input. Dock renderer must show exact worldfootprint; UI owns that acceptance.

Terrain boxes are actual simulation solids, not purely artwork. Module supplies deterministic12 centered x/y/z boxes per garden with w/d/h,material,solid,drillable,hp/maxHp. Shared renderer contract supplied to UI. Body radius12/height18; flight altitude52 clears24-high boxes but hits64/110 structures, othermodes altitude0. Movement subdivides into <=4unit steps, rejects each occupiedaxis and permits orthogonal sliding, stops blockedvelocity. Drill contact with positiveintent dt drains30hp/s,60hp deposits break in2scontact; neutralinertial contact causes no drilling. Destroyed solids remain as hp0/solidfalse bounded geometry, terrainbreak events identify id/material/x/y. Terrain recreated for checkpoint gardenentry (deliberately notmidgarden persistence).

Placement retains clear connected paths x110/480/850 and y90/485/515, verified all50gardens/allmodes. HOME, five stations and introspine remain reachable. Teleports rejectsolid footprints at currentmodeheight. New cargo spawn skipsoccupied locations; initial/recurring spectrum pickups move to nearest sidecorridor when otherwiseinside terrain. Garden terrain now initialized before spawning entrycargo.

Voice API: dock adds actualmatching count including0; reset adds removed1/remaining. shieldchange reports color,actualinserted added (newcellorder),removedcountmap,before/after counts, emitted onlyduring recycling withactualchanges. Resistance-only packets do notannounce insertion. Existing enqueue/recycle events retained. Timercycle max(12,20-.5violetcount), nonzero countdown untouched by ordinarycount changes, violetpaused retained, old checkpointtimer3s restored as3s. Voice owns actual10s crossing detection.

## Validation
Final tests againstoriginalproduction engine:93run81pass12fail, exit1, engine-red-final.txt. Initialfailed-first log engine-red.txt; later pickup-in-solid regression failed beforeitsfix, terrain-pickups-red.txt. Finalcandidate93pass0fail exit0 engine-green.txt. Eleven new tests: four shield/docking/timer, seven terrain. Existing82engine/shield tests preserved with deliberate contractupdates: two dockevent expectedobjects includecount0; standalone trailhistory fixture clears terrain to test full unobstructed168unit tail; guardian dodge chooses freeverticalescape rather than horizontally into actualtowers (realmovement/lockedwarning/damage assertions preserved). No productiontests removed.

Parent remaining: independent exacthash prewrite review; combinedfrozenUI/voice integration, browserclearfootprint/3Dprojection/audioacceptance, safeapplication andautomaticGit review. No fullhumanbalance or volumetricbackgroundphoto claim.
