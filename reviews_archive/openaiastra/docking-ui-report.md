# Docking UI candidate — 2026-09-08

Frozen candidates: game.js, index.html, style.css, ui.test.cjs. Exact old/new SHA256 in ui-manifest.json and four adjacent .diff files. No production writes or commits.

White benefit/red harm rings and diffuse auras surround colored pickup shapes. Platforms have eight-sided raised metallic surfaces, front sidewalls, cast shadows, bevels, rivets, biome metal/inlays, colored landing guides and progress rings. Dock events trigger a 0.55s settle, quiet three-layer mechanical/harmonic sound and docking text; departure gives lift, wake, launch label and rising harmony. Parked ship exhaust idles. Reduced motion suppresses physical lift/wake but keeps explicit status. Warp label is parenthesized outside shield, flips below at top edge, clamps horizontally, and reads paused on violet platform. HUD mirrors the paused status.

EmberMusic initialized only within existing user audio unlock, shares existing AudioContext and receives actual garden index plus playing/global mute/independent music mute/hidden state. Pause, finish, visibility and both mute toggles update immediately; all frames synchronize music. Repeated resume never creates a second player. Music setting persists. Existing synthesized effects now track bounded voices (48 cap), disconnect on end and cancel immediately on global mute/pause/hidden/pagehide. Music owns only its voices. Missing music module does not block gameplay. All stylesheet and script cache URLs docking-1; music.js before game.js.

Test-first UI red receipt ui-red.txt fails the absent red aura against original UI. Parent browser feedback requested stronger dimensional depth and aura spread; ui-depth-red.txt proves missing compact-scale face height before increasing it to24px with metal gradients and diffuse37px aura. Final node ui.test.cjs passes 19 assertion groups including all prior controls/checkpoint/FIFO/atlas/spectrum checks and new actual canvas calls, dock/undock effect audio, active-sound cancellation, and music lifecycle contract. Additional later cases cover persisted Music mute, top-edge countdown, platform depth and reduced-motion cue. ui-green.txt records final pass, node --check game.js exit0. Music integration uses an explicit fake player at the UI boundary; music module author independently tests real scheduler. No claim of human listening or browser visual acceptance in this report.

Source navigation used native jCodemunch resolve and file outline; exact known source reads for editing. Test-first and verification skills applied. Independent review/canonical write/automatic Git review remain parent responsibilities. QA files, if present, are separate scratch fixtures and must not ship.
