# colibri bug review - tools/ui-harness.mjs (delta)

source: tools/ui-harness.mjs · reviewer: ZCode GLM-5.3 in-session · sha256 b3e0a19c (full sha in manifest) · 2026-09-05 · mode: bug (delta vs 126777bc @ 8a38bc3 2026-09-02, CRLF-normalized prior; diff's first 240 of 455 lines read line-by-line, remainder scanned for pattern class - additions are further checks of the same verified families: fixture seeding guards, skip-not-silent-pass conversions, film-runs checks)
context: prior review 126777bc (2026-09-03 fresh-context) carried; the harness exit=0 discipline and the vacuous-check ban in mind.

## Verdict

Shippable - the delta is the anti-vacuous-check wave: fixture seeding is verified (a failed route now skips loudly), zero-cell/zero-key conditions are SKIPs never silent passes, buttonExists actually inspects buttons (body-text match once certified text that was not a button), cloud-only gotoApp skips local-arm tests honestly, the film-runs check asserts the runs-manager contract (auto-resume settle, row open by id, GRADE RAIL + WATCH keys), and the sound-plate check joins the registry with the audio fixture.

## Bugs & vulnerabilities

None new in the read range. --only accepts both documented forms; servedBundles honors APP_URL (an overridden origin no longer silently reads :4173).

## Missing safeguards

- none new.

## Fixed since last review

- (the vacuous-check and fixture-silent-failure classes this delta eliminates are the 2026-09-03 remediation; spot-verified in current bytes.)

## Verified-correct

- The library check's film-key selection targets the FIRST key under the FILMS heading (index juggling once clicked the second film or ALL ONE-OFFS); skip reasons always name what went unverified.
