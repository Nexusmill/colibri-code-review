# Synthesis - the VRAM pre-flight wave (2026-09-06)

Six single-file units in bug mode; one synthesis:

1. **[VERIFIED] The estimate's honesty chain** - corpus parse (newest bench
   entry, prefix-safe) -> live free (503/degraded = away, zeroed = not
   data) -> fixed verdict vocabulary with the basis spoken in every line.
   The live route answered `no-measurement` truthfully (the machine has no
   bench entries yet - benches need the backend up).
2. **[VERIFIED] Never a gate** - the queue's preflight fetch is swallowed
   on failure, rides both job exits, and the deck speaks only over/tight;
   fits and away stay silent. No new classes; danger register for over.
3. **[NAMED] Visual verification deferred honestly** - a real tight/over
   state needs a live backend plus a benched bundle; the line is
   idiom-reuse in the status well and will show on this rig's first real
   over. No fabricated state was staged for a screenshot.

Evidence: tsc clean, vitest 432/432 (5 new), harness fast tier 57/0/4
(vram-preflight PASS live through the proxy).
