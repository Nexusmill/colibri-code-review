<!-- source: src/vram/preflight.ts | reviewer: glm-5.3-zai-in-session | sha256: 71a04a5a9fa62f1bc0d6cbf2349d56e029da0080bfd222a6e3ea664907786e29 | date: 2026-09-16 | mode: bug -->
<!-- context: owner commission 2026-09-16: colibri bug hunt on the NEXT top ten files by import PageRank (the board past the hunted ten); fresh-context subagent per file under the 0.3.0 doctrine (prior review = context, never a skip; same-sha files hunt beyond the record). -->

## Verdict
Shippable with one new cross-file nick. Same-sha lineage hunt beyond both prior records (c23ec716: heading contract fix, prefix-confusion, free<=0-as-no-data, 15% thresholds; 71a04a5a: eta newest-wins, cold-load non-leak, etaMs on all five paths). The new ground is the DECOUPLING of the two parsers' "newest" choices against the writer's vram-optional footprint sentence — neither prior review connects these.

## Bugs & vulnerabilities
- **[MEDIUM] measured and etaMs can come from different bench entries, and etaMs's basis date is silently discarded** - `lines 37, 57, 72/79/84/87/89` + writer `vite.bench.ts:118`. What: `parseBenchFootprint` advances `best` only when the section has a `Torch: allocated` match (line 37), while `parseBenchEta` advances on medians — which the writer emits UNCONDITIONALLY (`median ${medianA}ms ... median ${medianB}ms` are always in the body), whereas the footprint sentence is conditional: `${vram ? ` VRAM footprint after load: ...` : ""}` (vite.bench.ts:118). Trigger: run a bench while the /caliper/vram fetch fails or counters are degraded mid-run (runBench keeps `vram` null) with an older footprint-bearing entry for the same bundle already in the corpus. Impact: `measured` falls back to the OLD entry (its date spoken — locally truthful) while `etaMs` comes from the NEWEST entry; `parseBenchEta` even computes `at` (line 57) and `preflightVram` drops it at every `etaMs: eta?.ms ?? null`. The interface (line 20) carries no eta date, so the queue's bench-median ETA (commit 9102b79, "running jobs carry a bench-median ETA") rides undated from an entry the spoken basis never names — a truthful-basis nick under the owner's status doctrine. Fix: thread `etaAt` into `VramPreflight` and speak it, or select both figures from the SAME entry (single pass: newest entry with both alloc and medians; fall back per-figure with each date spoken). CONFIRMED as code-level decoupling by reading both sites end to end; the rendered queue line itself is PLAUSIBLE (unverified because the queue/deck consumer's etaMs rendering was not re-opened in this pass — context budget).

## Missing safeguards
- No NaN guard on the corpus-derived number: `Number(alloc[1])` (line 37) — `[\d.]+` admits multi-dot strings ("6.4.1" → NaN), and NaN falls through both thresholds into `verdict: "over"` with `say: "over: ~NaN GB measured"` — visible nonsense rather than silent, but no validation rejects it. The machine writer emits toFixed(1), yet the corpus is also hand-appendable (real manual entries exist, e.g. `## LTX 720p 10s full render (seed 42, manual)`).
- etaMs has no spoken basis anywhere in the interface (measured.at is always spoken; eta's `at` is parsed at line 57 then discarded) — interface-level root of the finding above.
- The heading-date regex `— (\d{4}-\d{2}-\d{2})` (lines 36, 56) yields `at: ""` for real corpus shapes like `— recovered 2026-08-19` (measured-results.md line 37); it degrades gracefully to "date unknown", but the regex could accept an optional word between em-dash and date to keep the basis.

context-pack: jcodemunch local/Caliper outline + content (vite.bench.ts runBench/appendMeasured bytes, tools.ts summarizeVram line, preflight.test.ts contract lock), real knowledge/measured-results.md headings (37 heading lines, 135 "median" lines), git log 9102b79/6cc6391, both prior colibri reviews.
new-findings: 1
