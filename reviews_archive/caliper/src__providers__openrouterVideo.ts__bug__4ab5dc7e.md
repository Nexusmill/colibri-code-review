<!-- source: src/providers/openrouterVideo.ts | reviewer: glm-5.3-zai-in-session | sha256: 4ab5dc7ea05ea09d41c585c45d98c54651ff98cabbaa5bab9778acced34a267 | date: 2026-09-16 | mode: bug -->
<!-- context: owner commission 2026-09-16: colibri bug hunt on the NEXT top ten files by import PageRank (the board past the hunted ten); fresh-context subagent per file under the 0.3.0 doctrine (prior review = context, never a skip; delta files report new/changed only + close the loop). -->

## Verdict
Shippable — CONFIRMED clean delta. The file's only change since the prior-review bytes (sha c09230c4, commit 3f395f0) is commit 2b5b7f9's additive `end_image` synthesis block (lines 97-103); traced end-to-end (schema → ModelPicker generic field render → runOrVideo's dual-spelling `endImage ?? end_image` data-URI frame handling in vite.openrouter.ts), it introduces no defect. Note for the record: the late-August "resolution snapping, cost parsing, model listing" waves landed in sibling files (vite.openrouter.ts and friends), not in this one — this file's price/cost code is byte-identical to what the prior clean pass reviewed.

## Fixed since last review
- Prior bug pass (2026-08-27, sha c09230c4) recorded **zero findings** ("Clean") — nothing to close. Its two encoded invariants re-verified in current bytes: the page-price summary takes the FIRST "from $X/second" with an early `break`, never min-of-page (lines 141-144, unchanged); sku normalization keeps token-priced models honest (`perSecondUsd: null`, lines 55-67, unchanged).
- Loop closure with the sibling FEATURE review (same sha, 2026-09-05): its "last_frame synthesis" suggestion — `properties.end_image` gated on the same list — has now LANDED, exactly as sketched, with a covering test (src/providers/openrouterVideo.test.ts:86-93: both-spellings engine gets `image` + `end_image` + `frameImages: ["first_frame","last_frame"]`; first-only engine gets no `end_image`) and consumer wiring at both call sites.

## Bugs & vulnerabilities
None found in the delta. The changed block was adversarially verified: gating reads the already-filtered `frameImages` list (only `first_frame`/`last_frame` survive line 93), the property shape mirrors the proven `image` property, and every downstream consumer either iterates schema properties generically (ModelPicker.tsx:321-331) or explicitly accepts both key spellings (vite.openrouter.ts runOrVideo, `endImage ?? end_image`). Candidate defects traced and refuted: silent-drop of non-data-URI frames (pre-existing class, owned and documented in vite.openrouter.ts's own comment block — not this file's code); `cents_per_*` sku unit confusion (the miss direction fails toward `null`/"—", the honest direction); NaN price text in `mapOrImageModel` (guarded upstream by the `parseFloat(...) > 0` filter at its sole call site, vite.openrouter.ts listImageModels).

## Missing safeguards (bullets)
- Schema/transport tension, cross-file (owner of the gap: vite.openrouter.ts): `synthesizeOrSchema` declares `image`/`end_image` as `format: "uri"` — inviting any URI — while runOrVideo's transport only accepts `data:` URIs and silently omits anything else. A visible "paste a data URI or pick a file" error at the run boundary would beat silent omission; the schema itself cannot express the constraint.
- `normalizeOrPerSecond`'s `/duration_seconds|cents_per/` match would fold a hypothetical `cents_per_image` sku into the per-second floor at n/100 (unit confusion). PLAUSIBLE only — unverified because no such sku shape has been observed on video models; today's miss-direction (`cents_second` etc.) safely yields `null` rather than a wrong number.
- `mapOrImageModel` (lines 189-190) formats `Number.parseFloat(p.image_output)`/`p.prompt` with no finite-check of its own — a malformed catalog string would render "$NaN/M tokens". Guarded today only by the caller's `> 0` filter; a second guard inside the exported function would protect future callers.
- `end_image` is exempt from `required` by design, but nothing downstream warns when a last_frame-capable engine is run with no ending frame chosen — an intentional no-op, only worth a nudge if the film flow's continuity bookend ever becomes mandatory.

context-pack: jcodemunch local/Caliper find_importers (vite.openrouter.ts, src/api/catalog.ts, src/api/openrouter.ts, test) + search_text end_image across repo; vite.openrouter.ts runOrVideo frames block + poll loop read; ModelPicker.tsx generic SchemaField rendering; git log/diff 3f395f0→2b5b7f9 proving the single-block delta; prior bug + feature artifacts at sha c09230c4.

new-findings: 0
