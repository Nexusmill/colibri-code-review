<!-- source: src/install/tiers.ts | reviewer: glm-5.3-zai-in-session | sha256: e622496ba2c94bac6870f123fdeee648ddc13d2a0a7c5bf47c0133eb872a1feb | date: 2026-09-17 | mode: bug -->
<!-- context: owner commission 2026-09-17: BUGHUNT12 - colibri bug hunt on the next ten files by import PageRank (ranks 21-30); fresh-context lineage pass beyond the recorded review at these exact bytes (0.3.0 doctrine: prior review = context, never a skip). -->

## Verdict
Sound beyond the prior review. The file is byte-identical to the 2026-09-03 review (161 lines, 2 commits, untouched since 2026-09-02 06:17) and that review's four findings all remain open in this file (none in the remediation manifest) — they are context, not re-reported. One line of the prior review is now closed OUT of file: the MEDIUM StatusFooter rounding twin is fixed in code (StatusFooter.tsx:6 imports `fmtGb`; its local `gb` is now `fmtGb(n / 2 ** 30)` with a comment citing the 2026-09-03 bug hunt), though it never got a manifest row. This pass re-verified the load-bearing math and contracts independently: floors are inclusive and EXACT at byte-perfect values (division by 2^30 is power-of-two scaling, so `15 * 2**30 / 2**30 === 15` compares exactly against `VIDEO_FLOOR_GB`; same at 8/32/64 — matching the boundary tests); a degraded VRAM read (`vram_total` absent, 0, or NaN — `NaN > 0` is false) skips the device and can only ever LOWER the verdict, never inflate it; the probe contract with its only production caller (vite.caliper.ts:445-477) is faithful (backendUp set only after a parsed system_stats; vram_total number-checked; name cleaned), and all eight LADDER ids still resolve in catalog.ts after the 2026-09-16 byte re-grounding (Wan22-I2V-FP16/FP8 :177/:167, FLUX.2-Dev :196, Qwen-Image-2512-FP8 :187, LTX-2.3-Distilled :147, Anima-Aesthetic-v1.1 :227, Anima-Turbo :238, brain qwen3-4b-brain :59); both GB displays of the verdict number (wizard FirstRun.tsx:237 on GiB, StatusFooter on bytes/2^30) now truncate through the one rule. One new defect found, comment-only; no runtime defect at these bytes.

## Bugs & vulnerabilities
- **[LOW] The module header documents the pre-register three-rung ladder, not the approved five-rung owner law** - `tiers.ts:1-8`. CONFIRMED (textual + lineage). What: the header purports to describe "The GPU tier ladder" but narrates only video-floor → image → cloud ("below it local image is what fits; below that, or with no CUDA device at all, the cloud is the honest engine", lines 4-7) with no mention of the video-pro/video-max registers (floors 32/64, lines 74-95) — two of five rungs carrying four of the seven packages and the entire upper half of the owner law the commission dates 2026-09-07. Trigger: any editor grounding floors or doctrine from the module summary alone. Evidence it is stale residue, not a deliberate scope: the registers landed in the second of the file's exactly two commits (2026-09-02 06:17, fe8f967 — HISTORY Era 51 addendum "two owner rulings in one morning"), the header's "Adding a **bundle** is data in catalog.ts" (line 7) is the pre-addendum wording while the addendum's parallel comment (line 62) says "Adding a **package**", and the header's machine-measurement sentence duplicates the older phrasing the LADDER comment carries as "the distilled LTX". Impact: none at runtime — the LADDER constant and the 32/64 tests are the operative law; the drift misleads only the next human at the file's front door. Fix: widen the header to name all five rungs (or cut the rung narration and point at the LADDER comment) and align "bundle"→"package".

## Missing safeguards
- `judgeTier` returns `packages: rung.packages` (line 152) — a live alias of the module-level LADDER rung's array. The vite middleware process is long-lived and calls judgeTier per /api/install/recommend; one future consumer mutating a verdict's packages (push/splice) would corrupt the LADDER for every subsequent verdict in that process. Verified no current caller mutates (vite spreads into a new plan object; JSON.stringify and the tests only read; the browser gets a deserialized copy) — latent, so a safeguard, not a bug: return a shallow copy (`[...rung.packages]`) or `Object.freeze` the rungs.
- The prior review's three safeguards remain open at these exact bytes and still stand (not re-litigated): `fmtGb` has no `Number.isFinite`/negative guard on its exported surface; LADDER-id resolution is enforced only by test, with the production `if (manifest)` at vite.caliper.ts:464 silently dropping a drifted id; the terminal-rung `!` at line 145 relies on the floor-0 rung with no explicit invariant.

context-pack: jcodemunch outline+importers (5: FirstRun.tsx, StatusFooter.tsx, install/client.ts, tiers.test.ts, vite.caliper.ts); recommend endpoint + probe mapping read (vite.caliper.ts:433-478); all 8 catalog ids re-verified post-2026-09-16 re-grounding; churn 2 commits 2026-09-02 (03:51/06:17) matching HISTORY Era 51 base + upper-register addendum; prior review e622496b + remediation manifest (no tiers.ts rows) ingested.
new-findings: 1
