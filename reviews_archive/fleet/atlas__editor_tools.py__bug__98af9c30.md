# Colibri review - atlas/editor_tools.py (bug, lead Phase-3 verification of the 2026-09-10 fork; DOCKET disposition)

- source: `C:\Users\User\source\repos\fleet\atlas\editor_tools.py`
- model: claude-opus-5 (in-session lead; the 2026-09-10 fork report `_external_raw/atlas__editor_tools.py__fork-2026-09-10.md` is INPUT)
- sha256 reviewed: `98af9c307207bf61ac9e0a49ceeb26df081dc0d0108454ec01e7be06ad60eb47` (identical to the bytes the fork reviewed on 2026-09-10 - every fork line citation still holds)
- date: 2026-09-15
- mode: bug (Phase 3 lead pass; retirement test applied - fix now only if the defect costs data/money/secrets or blocks the pipeline BEFORE the code retires or is rewritten in Phase 3; otherwise docket with the call recorded)
- context pack: `find_importers`: agency_graph (merge_video_audio_logic) + agent_runner (merge_video_audio) - LIVE on the song pipeline; existing row FLEET-P0-EDITOR-CACHE-KEY; fork probe re-run today: RED (two same-basename Replicate URLs -> one cached file).

## Verdict
The existing HIGH (`download_if_url` keys its never-expiring cache on the URL basename; Replicate delivery URLs share generic basenames, so a later clip gets a stale [CACHE HIT] and the editor merges the wrong footage) is re-verified PRESENT on current bytes. Retirement test: it costs money only when the fleet runs end-to-end - which cannot happen before the Phase 3 provider factory, and the editor node is rewritten first thing in Phase 3 per the row itself. NOT re-fixed here (G35 verify-before-refix: the row's plan stands); the row is amended with today's re-verification and the fork's missing-safeguard note (the HTTP branch is uncapped and unrestricted - same class as the asset_manager URL branch recorded in unit 4).
