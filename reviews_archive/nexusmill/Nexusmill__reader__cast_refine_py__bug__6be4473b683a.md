# scan 6be4473b683a (batch batch-1790089108-GXtCAG9xkxytviXCXMls via z-ai/glm-5.3:batch)

Reviewed all eight modified files against the regression suite, hunting specifically for what the two prior gates could not have caught: error-path interactions between the new resume/recovery logic, cache-consumer asymmetries, and process/queue edge cases. Verified clean (no findings): the atomic-retry marker writes and per-batch key derivation in `cast_refine`; split/repair recursion bounds in `ask_refined`/`ask_styles`; batch partitioning, skipped-utterance and stats accounting; the reasons-deque ↔ uncertain-item mapping (correctly handles duplicate span texts); `phoneme_pieces` termination and ordering; `index_worker` oversized-line drain and temp/final cleanup; QProcess lifecycle paths (cancel/kill/`_exited`/`_error`) in both clients; pin/unpin pairing around publish; suspend/resume analysis gating including the survived-kill path; quit-join behavior.

VERDICT: BLOCK
1. **reader/speech/client.py — `SpeechClient._pump`: a corrupt or unreadable cached WAV permanently wedges narration for that sentence.** On a cache hit, `sf.info(str(path))` runs inside the `try`, but the only handler is `_fail("Local model or audio cache is unavailable. Check setup and retry.")` — the bad entry is never discarded (no `discard_path`/unlink), so every retry re-hits the same file and fails identically; that text+voice can never be spoken until the cache file is deleted by hand. `_fail` also clears and fails the entire queued batch, and the message points the user at "setup" instead of the cache. Every sibling consumer self-heals this exact case — `IndexSpeechClient._pump` validates its cached audio and unlinks + resynthesizes, and `analyze_styles`/`analyze_refined` re-ask on a corrupt section file — the Kokoro playback path is the only cache reader with no corrupt-entry recovery. Fix: on lookup-hit validation failure, discard the entry and fall through to synthesis, as `index_client` does.

2. **reader/cast_ui.py — `load_cached_cast`: a completed refined scan is never recovered from `refined/result.json`; after a store-write failure the code silently falls back to the legacy scan.** `refined/result.json` is write-only from the UI's perspective — the only reader of a completed scan is `narration_store.analysis(book)`. If `_save_detected_cast`'s `apply_analysis` fails (explicitly caught and reported as "Could not save detected cast"), the finished, version-current result is stranded: on the next load — or immediately, since `_cast_scan_finished` calls `load_cached_cast()` — every refined branch is skipped (`resuming` requires result.json to be *absent*, and the marker version matches), the code drops into legacy `analyze_book`, and if the legacy section cache is complete it applies the lower-quality legacy result in place of the finished refined scan. Note the refined result is the larger document (attribution + utterances + index), so a disk-full failure that rejects it can still accept the smaller legacy one — a silent downgrade of a completed improved scan, with only the generic "Earlier scan: use Scan / improve characters…" hint. With an incomplete legacy cache the user is instead told to scan again despite a complete refined result sitting on disk. Fix: before the legacy fallback, version-gate and load `refined/result.json` and apply it.

3. **reader/index_client.py — `IndexSpeechClient._read`: durable-cache publication leaks partial files and misreports an already-cached clip as failed.** `pending = target.with_suffix(".pending"); shutil.copyfile(path, pending); os.replace(pending, target)` has no cleanup on failure — if `copyfile` or `replace` raises (disk full on the growing `index_audio` directory being the realistic case), a partial `key.pending` file is left in the durable directory forever; nothing anywhere creates, reads, or removes `.pending` files except this one unprotected block, so junk accumulates and aggravates the disk-full condition that produced it. Additionally, `path.unlink(missing_ok=True)` on the worker's temp WAV runs *after* `os.replace` succeeded: if it raises (e.g. antivirus briefly holding the fresh WAV on Windows), the `except` clause reports `_fail("IndexTTS could not narrate this passage…")` — the active job and the whole queue are failed and the process killed even though the clip was already published to the durable cache; a later retry heals via the cache, but the run was aborted and misreported. Fix: wrap `pending` in try/finally (unlink on failure), and treat a post-replace temp-unlink failure as success since the clip is already cached.
