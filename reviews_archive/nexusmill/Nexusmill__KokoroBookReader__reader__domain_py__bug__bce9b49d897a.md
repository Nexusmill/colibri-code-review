# scan bce9b49d897a (batch batch-1790101391-1ArJ0v92lIBDSQ5NHmGV via z-ai/glm-5.3:batch)

Cross-family scan complete. Coverage notes per file:

**domain.py** — Frozen dataclasses only; field types/defaults consistent with all consumers seen. No logic to defect.

**narration.py** — Verified: `__init__` structural validation and the landed self-heal (non-str entries skipped, per-entry OSError/ValueError isolation, basename-only re-pointing — no traversal); atomic `save()` with tmp cleanup in `finally`; `_commit_data` rollback on failure; `validate()` bounds (1–50 parts, key-set exactness, emotion ≤500, dual join-normalization for mid-word splits); `apply_analysis` evidence bounds, narrator fallback (`next(iter(voices), None)` + None guard), casting seeding, merge re-application, and the `self.data` swap around the validation loop so `assigned_voice` sees candidate casting; `merge_character` cycle handling (self-loop case `merges[A]=A` is caught by the verification pass before any commit; all mutation happens on a deepcopy and nothing persists on raise); `_merge_target` cycle detection; `directions` manual-vs-analysis precedence and hint application keyed on identity excluding emotion (no feedback loop with `apply_styles`). `audio_identity`/`add_voice` raise loudly on missing/invalid samples, which callers in the shown UI wrap in `except Exception` dialogs — fail-loud, not silent.

**app.py** — Verified: `data_root()` migration guarded by existence check, env override, OSError→RuntimeError with actionable message; `root` stays `None` if `data_root()` raises, so the startup-error write is correctly skipped and the message box still shows; `QLockFile` with stale-time 0 (PID-liveness still removes dead-process locks; `unlock()` no-ops when not held, so the early `return 2` path can't release another instance's lock); `controller` closure guarded for `None`; finally-block ordering (batch.stop → speech.close → player → library → lock) with the speech-None fallback closing kokoro/index individually.

**book_index.py** — Verified: seed-record validation (index bounds, evidence substring after quote normalization, alias mutual-mention); utterance closing heuristic with the single-bare-quote guard; `_attribute` quote-split on `before`, coordination guard (`and|or` after AFTER matches only), single-name consensus, evidence sentence selection; `request` budget accounting (required sentences admitted unconditionally and the post-loop `size>limit` raise only fires when required text alone exceeds the limit — optional sentences can never push past it), paragraph-neighborhood slicing, mention top-up bounded by `limit` and capped at 40 names; `keys.index(key)` cannot miss (utterance keys derive from sentences); defaultdict accesses never create spurious keys.

**audio/cache.py** — Verified: key regex (64-hex) and tmp-cleanup regex (32-hex) match the actual producers (`sha256.hexdigest`, `uuid4().hex`); `_path` symlink/non-file/resolve containment checks block linked entries; `lookup` validates 24kHz/mono/frames>0 inside the try with `os.utime` for LRU touch; `publish` validates before copy, `os.replace` atomic, tmp cleaned in `finally`; eviction is oldest-first, pinned-skipped, size-accounted after successful unlink; `discard_path` confines deletion to hash-named `.wav` entries directly under root — no traversal.

**audio/player.py** — Verified: token/media identity guards on all closures; `pending_offset` consumed only by the current media (stop() clears it and bumps token); seekableChanged re-evaluation; failure path retries exactly once (keepalive disabled permanently first, so a second failure emits `failed`); retry captures `play_requested` before `load()` (which resets it via `stop()`) and restores it after; `stop()` detaches output and `deleteLater`s; `Silence` UInt8 vs signed/float zero byte; `OutputLease` device-id/error/state reuse check.

**index_panel.py** — Verified: Save/assign disabled when no book (so `set_segments`/`assign_voice` never see `book=None`); `set_segments` exceptions keep the dialog open with a warning instead of silently accepting; voice-name refresh after import keeps the list in sync with the store; preview player parented to the dialog and stopped on `finished`; `findItems` miss is non-fatal; INDEX_VOICE re-append after `super().refresh_voices()` with `was_index` restore.

Items considered and rejected as non-findings: `EXCLUDED` lacking he/she/they/I (attribution filtering depends on `clean_name`/`mentioned` in cast_scan.py, which is queued separately — not establishable from these files); `_evict`/`clear` not catching `PermissionError` on unlink (requires an over-budget cache plus an externally locked unpinned hash-named file, and the next `lookup`/`publish` self-heals); `add_voice` `SameFileError` only reachable with a relative `KOKORO_DATA_ROOT` re-importing the store's own file; corrupt-JSON fail-fast in `NarrationStore.__init__` (deliberate, surfaces via the startup-error path).

VERDICT: CLEAR
