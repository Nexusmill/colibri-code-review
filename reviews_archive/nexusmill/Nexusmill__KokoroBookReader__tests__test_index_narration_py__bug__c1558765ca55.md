# scan c1558765ca55 (batch batch-1790111331-fo1MxzkZGrm8pX9kBf2s via z-ai/glm-5.3:batch)

VERDICT: BLOCK

1. **tests/test_index_narration.py:22** (`test_script_preserves_original_and_roundtrips`) — `assert book().sentences[0].text == "Wait. Please stay."` is a tautology: `book()` constructs a brand-new Book from string literals on every call, so the assertion compares the literal to itself; the Book instances actually passed to `set_segments` are discarded and never inspected. Trigger: always true, regardless of code under test. Impact: a `NarrationStore.set_segments` that rewrites/normalizes book sentence text passes; the "preserves original" half of the test is dead. Fix: hold the instance — `b = book(); s.set_segments(b, 0, [...])` … then `assert b.sentences[0].text == "Wait. Please stay."`, and additionally assert the persisted segment text still equals `b.sentences[0].text`.

2. **tests/test_index_narration.py:265,270** (`test_index_window_retains_text_and_position_while_generating`) — `text` is snapshotted from `window.book_view` *before* `generate_button.click()` (line 265); after the ready emission the stale snapshot is only compared to `controller.book.sentences` (line 270) — the view is never re-read. Trigger: generation rewriting or disabling view items (e.g., per-item placeholder/"generating…" text). Impact: the assertion cannot fail on any view-content change after the click; only book-model mutation is caught, so the "window retains text while generating" contract is untested for the widget it names. Fix: after `engine.ready.emit(...)`, re-read `[window.book_view.item(i).text() for i in range(window.book_view.count())]` and assert it equals both the pre-click snapshot and `[s.text for s in controller.book.sentences]`.

3. **tests/test_speech.py:51-54** (`test_cache_keys_include_voice_but_not_playback_rate`) — the name claims rate-independence, but no assertion involves playback rate; only voice and text sensitivity are checked. Trigger: adding playback rate to `cache_key` (the exact regression the name advertises guarding). Impact: test stays green while rate changes begin thrashing the audio cache — false confidence in an untested invariant. Fix: assert key equality across rate values if rate is (or ever becomes) a `cache_key`/lookup input; otherwise rename (e.g., `test_cache_keys_include_voice_and_text`) so the unverified claim isn't advertised.
