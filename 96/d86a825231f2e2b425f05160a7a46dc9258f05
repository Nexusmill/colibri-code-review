{
 "type": "CLEAR",
 "commit": "96d86a825231f2e2b425f05160a7a46dc9258f05",
 "caller": "zcode",
 "when": "20260923-191932",
 "files": {
  "agent_error_rates.py": {
   "sha": "f238e0692daa6b6288e99fe35d4db6744166e5a8dde3dff15837eb8c771753c2",
   "model": "deepseek/deepseek-v4-pro-0813@deepinfra/fp8+wafer+ionstream",
   "when": "20260923-191401",
   "artifact": ".adversary\\reviews\\gate_20260923-191401.md"
  },
  "docs/agent_error_rates.json": {
   "sha": "bed0d2256cfb292f7ada214ed917a2d07b5a4984a954c45be742b3b8e36f1f44",
   "model": "deepseek/deepseek-v4-pro-0813@deepinfra/fp8+wafer+ionstream",
   "when": "20260923-191401",
   "artifact": ".adversary\\reviews\\gate_20260923-191401.md"
  },
  "tests/test_agent_error_rates.py": {
   "sha": "3d1d2847ad04f248d669b5f30584904d7f1d13a5bad582e84cc5720cb89b03e5",
   "model": "deepseek/deepseek-v4-pro-0813@deepinfra/fp8+wafer+ionstream",
   "when": "20260923-191401",
   "artifact": ".adversary\\reviews\\gate_20260923-191401.md"
  }
 },
 "artifacts": {
  ".adversary\\reviews\\gate_20260923-191401.md": "model: deepseek/deepseek-v4-pro-0813@deepinfra/fp8+wafer+ionstream | provider: DeepInfra | usage: {'prompt_tokens': 19471, 'completion_tokens': 2420, 'total_tokens': 21891, 'cost': 0.0254603, 'is_byok': False, 'prompt_tokens_details': {'cached_tokens': 5120, 'cache_write_tokens': 0, 'audio_tokens': 0, 'video_tokens': 0}, 'cost_details': {'upstream_inference_cost': 0.0254603, 'upstream_inference_prompt_cost': 0.0191683, 'upstream_inference_completions_cost': 0.006292}, 'completion_tokens_details': {'reasoning_tokens': 0, 'image_tokens': 0, 'audio_tokens': 0}} | scrubbed: 0 | files: ['agent_error_rates.py', 'docs/agent_error_rates.json', 'tests/test_agent_error_rates.py'] | caller: zcode\n\nLooking at this change, I need to examine the new functionality being added: adjudication joining and validated statistics.\n\n**Critical issue found in `validated_stats`:**\n\nThe function receives `episodes` and computes `unadjudicated_episodes = len(episodes) - len(adjud)`. But in `main()`, it's called with:\n\n```python\n\"validated\": validated_stats(\n    [r for r in rows if r.get(\"source\") == \"gate-denial\"]),\n```\n\nThis filters to gate-denial rows only. However, the `by_caller` breakdown in `validated_stats` iterates over ALL episodes passed to it (which is correct - only gate-denial rows), but the `unadjudicated_episodes` count is computed as `len(episodes) - len(adjud)` where `episodes` is the filtered list. This is correct for the filtered population.\n\nBut wait - looking at the MD generation:\n\n```python\nv = ledger[\"validated\"]\n_n_arc = len(rows) - sum(1 for r in rows if r.get(\"source\") == \"gate-denial\")\nmd += [\"\", \"## VALIDATED true rate (adjudicated gate episodes only)\", \"\",\n       \"The percentage chance that a COMMIT introduces at least one VALIDATED \"\n       \"error, 0-100%% - measured over ADJUDICATED gate episodes only \"\n       \"(gate-recorded outcomes: fixed / rebutted-upheld / cleared-unedited / \"\n       \"rebuttal_rejected). Pre-recording gate history (%d episodes) is flagged \"\n       \"unadjudicated and never mixed in; %d archive-sourced episodes predate \"\n       \"the recording entirely and sit outside this measurement.\"\n       % (v[\"unadjudicated_episodes\"], _n_arc), \"\"]\n```\n\nThe `_n_arc` calculation is `len(rows) - sum(1 for r in rows if r.get(\"source\") == \"gate-denial\")`. This counts archive-sourced rows. But `rows` is the deduplicated list from `by_ep.values()`. The deduplication logic keeps gate rows when they have denial artifacts, but archive rows otherwise. So `_n_arc` correctly counts archive-sourced rows in the final deduplicated set.\n\nHowever, there's a subtle issue: the `unadjudicated_episodes` in the validated stats is computed from the filtered gate-denial rows, but the MD text says \"Pre-recording gate history (%d episodes) is flagged unadjudicated\". This is correct - it's the gate episodes without adjudication records.\n\n**Looking at the `join_adjudications` function more carefully:**\n\n```python\ndef join_adjudications(episodes, repo_root):\n    path = os.path.join(repo_root, \".adversary\", \"adjudications.json\")\n    by_commit = {}\n    try:\n        data = json.loads(open(path, encoding=\"utf-8\").read())\n        for ep in (data.get(\"episodes\") or []):\n            if isinstance(ep, dict) and ep.get(\"commit\"):\n                by_commit[ep[\"commit\"]] = ep.get(\"findings\") or []\n    except (OSError, ValueError):\n        pass\n    for e in episodes:\n        outs = by_commit.get(e[\"commit\"])\n        e[\"adjudicated\"] = outs is not None\n        e[\"outcomes\"] = outs or []\n    return episodes\n```\n\nThe commit keys in `adjudications.json` are matched against `e[\"commit\"]`. In `gate_episodes`, the commit is set to `c[\"sha\"]` which comes from `parts[0]` - the full SHA from `git log --format=%H`. The adjudications.json likely stores full SHAs as well, so this should match.\n\n**Potential issue with the `caller` field extraction:**\n\n```python\nhead = text.splitlines()[0] if text else \"\"\ncm = re.search(r\"\\| caller: (\\S+)\", head)\n```\n\nThis only looks at the FIRST line of the artifact. The comment says \"the gate stamps '| caller: <id>' at the END of the header line\". If the header spans multiple lines (unlikely given the format shown in tests), this would fail. But based on the test format, the header is a single line, so this should be fine.\n\n**Looking at the `GATE_TS_RE` change:**\n\n```python\n-GATE_TS_RE = re.compile(r\"gate_(\\d{8}-\\d{6})\\.md$\", re.IGNORECASE)\n+GATE_TS_RE = re.compile(r\"gate_(\\d{8}-\\d{6})(?:-\\d+)?\\.md$\", re.IGNORECASE)\n```\n\nThis now matches files like `gate_20260923-163305-2.md`. The timestamp group captures `20260923-163305` correctly. This seems fine.\n\n**Checking the `validated_stats` FP calculation:**\n\n```python\nif (f.get(\"outcome\") in (\"rebutted-upheld\", \"cleared-unedited\")\n        and not f.get(\"rebuttal_rejected\")):\n    fp += 1\n```\n\nA finding with `outcome == \"rebutted-upheld\"` and `rebuttal_rejected == True` is NOT counted as FP (correct - it's validated). A finding with `outcome == \"cleared-unedited\"` and no `rebuttal_rejected` IS counted as FP. This seems consistent with the docstring.\n\n**One potential issue:** In `validated_stats`, the `_valid` function checks:\n\n```python\ndef _valid(e):\n    return any(f.get(\"outcome\") == \"fixed\" or f.get(\"rebuttal_rejected\")\n               for f in (e.get(\"outcomes\") or []))\n```\n\nThis treats `rebuttal_rejected` as a standalone validation criterion. But what if a finding has `outcome == \"rebutted-upheld\"` AND `rebuttal_rejected == True`? The test case c3 has exactly this. The finding is counted as validated (correct - the rebuttal was rejected, so the finding is real). But it's also NOT counted as FP (correct - it's not a false positive). This seems consistent.\n\n**Looking at the deduplication logic interaction with adjudication:**\n\nThe deduplication happens AFTER `join_adjudications` is called on gate episodes. The `join_adjudications` mutates the episode dicts in place. Then in the dedup:\n\n```python\nby_ep = {}\nfor r in arc_rows + gate_rows:\n    k = (r[\"repo\"], r[\"commit\"])\n    cur = by_ep.get(k)\n    gate_wins = (r[\"source\"] == \"gate-denial\" and r.get(\"denial_artifact\")\n                 and (cur is None or cur[\"source\"] != \"gate-denial\"))\n    arc_earlier = (cur is not None and r[\"source\"] == \"archive\"\n                   and cur[\"source\"] == \"archive\"\n                   and (r[\"date\"] or \"9999\") < (cur[\"date\"] or \"9999\"))\n    if cur is None or gate_wins or arc_earlier:\n        by_ep[k] = r\nrows = list(by_ep.values())\n```\n\nThe gate rows have `adjudicated` and `outcomes` fields added by `join_adjudications`. Archive rows don't have these fields. When a gate row wins the dedup, it retains its adjudication data. When an archive row wins (because the gate row has no denial artifact), the archive row has no adjudication data. This is correct - the archive row predates the recording.\n\n**But wait** - there's a subtle issue. The `validated_stats` function is called with:\n\n```python\n[r for r in rows if r.get(\"source\") == \"gate-denial\"]\n```\n\nThis filters the deduplicated rows to only gate-denial rows. But what if a gate row was REPLACED by an archive row in the dedup? Then that episode's adjudication data is lost from the validated stats. However, this is correct behavior - if the gate row had no denial artifact (CLEAR-only), it shouldn't be in the validated stats anyway since it's not a denial episode.\n\n**Actually, I found a real issue:**\n\nIn `main()`, the `join_adjudications` is called on `eps` BEFORE the deduplication. But the deduplication can replace a gate row with an archive row. The archive row doesn't have `adjudicated` or `outcomes` fields. Then when `validated_stats` is called with the filtered rows, it only sees gate-denial rows that survived dedup. This is correct.\n\nBut there's another issue: the `join_adjudications` function is called with `root` which is the repo root. For linked worktrees, the `root` is the parent checkout directory (from `_git_identity`). The `.adversary/adjudications.json` would be in the parent checkout. This should be correct.\n\n**Let me check one more thing - the `caller` field in the episode dict:**\n\nIn `gate_episodes`:\n```python\n\"caller\": (blk[\"parsed\"].get(\"caller\") or \"\") if blk else \"\",\n```\n\nFor episodes without a BLOCK artifact (CLEAR-only commits), `caller` is empty string. In `validated_stats`:\n```python\nc = callers.setdefault(e.get(\"caller\") or \"unknown\", ...)\n```\n\nEmpty string becomes \"unknown\". This is correct - CLEAR-only commits have no caller attribution.\n\n**One more potential issue:** The `parse_gate_artifact` function returns `caller` as a string. But in the test:\n\n```python\ndef test_parse_gate_artifact_old_header_has_no_caller():\n    ...\n    row = aer.parse_gate_artifact(\"r\", p)\n    assert row and row.get(\"caller\") in (None, \"\")\n```\n\nThe function initializes `caller = \"\"` and only sets it if the regex matches. So for old artifacts, `caller` is `\"\"`. The test accepts both `None` and `\"\"`. This is fine.\n\n**Actually, I need to look more carefully at the `validated_stats` function's `by_caller` breakdown:**\n\n```python\ncallers = {}\nfor e in episodes:\n    c = callers.setdefault(e.get(\"caller\") or \"unknown\",\n                           {\"episodes\": 0, \"adjudicated\": 0, \"validated\": 0})\n    c[\"episodes\"] += 1\n    if e.get(\"adjudicated\"):\n        c[\"adjudicated\"] += 1\n        if _valid(e):\n            c[\"validated\"] += 1\n```\n\nThis iterates over ALL episodes passed to it (which is the filtered gate-denial rows). The `episodes` count in the by_caller breakdown includes unadjudicated episodes. This is correct - it shows the total gate episodes per caller, with the adjudicated subset broken out.\n\n**I don't see any critical bugs in this change.** The logic appears sound, the tests cover the key scenarios, and the live data in the JSON is consistent with the implementation.\n\nVERDICT: CLEAR"
 },
 "adjudication": {
  "denial": "reviews/gate_20260923-190934.md",
  "outcomes": {
   "fixed": 2
  },
  "detail": ".adversary/adjudications.json"
 }
}
